# System

- [ ] Mermaid -> After switching from D2 to mermaid, it loads indefinitely
- [ ] D2 -> Loads indefinitely when graph is not x -> y. It cannot render anything else apparently
- [ ] Png saving

# UI

- [ ] Ribbon menu doesn't take the whole width
- [ ] App header too big, displays the name of the app for no reason and doesn't display close button
